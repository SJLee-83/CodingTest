# <sstream>

- stringstream
- istringstream
- ostringstream

# <algorithm>

- min
- max

# <climits>

- INT_MAX
- INT_MIN

# <string>

- to_string
- stoi // int
- stol // long
- stod // double

# DFS

- 배열 길이를 N이라 하면, A와 B를 짝짓는 모든 경우의 수는 N! (팩토리얼)
- 제한사항의 숫자는 "어떤 알고리즘을 써야 하는지" 알려주는 힌트입니다. N이 1,000이면 O(N²)까지는 여유롭고 O(N log N)이면 아주 편안하죠. 반대로 N이 10~20 정도로 작았다면 그때야말로 DFS/백트래킹을 의심할 타이밍

# 오버플로 확인

- 원소가 최대 1,000이고 개수도 최대 1,000이니 합의 최댓값은 1,000 × (1,000 × 1,000) = 10⁹입니다. int의 한계인 약 21억보다 작아서 안전해요. 다만 제한이 조금만 컸어도 넘쳤을 거라, 아슬아슬한 문제에선 long long을 쓰는 게 안전합니다.

# 알파벳 대소문자 변환

## A - touppper / tolower

'''
#include <cctype>

s[i] = toupper(s[i]); // 대문자로
s[i] = tolower(s[i]); // 소문자로
'''

## B - 아스키코드 연산

'''
s[i] = s[i] - 32; // 소문자 → 대문자
s[i] = s[i] + 32; // 대문자 → 소문자

s[i] = s[i] - 'a' + 'A'; // 위와 같지만 의미가 더 명확
'''

# 십진수 -> 이진수

원리는 2로 계속 나누면서 나머지를 모으는 것입니다. 6을 예로 보면요.

6 ÷ 2 = 3 ... 나머지 0
3 ÷ 2 = 1 ... 나머지 1
1 ÷ 2 = 0 ... 나머지 1

나머지를 나온 순서대로 적으면 0, 1, 1인데, 실제 답은 "110"입니다. 나머지는 낮은 자릿수부터 나오기 때문에 뒤집어야 해요. 이게 이 변환에서 가장 많이 틀리는 부분입니다.

'''
// 방식 1: 앞에 붙여나가기 (뒤집을 필요 없음)
string toBinary(int n) {
string result = "";
while (n > 0) {
result = char('0' + n % 2) + result; // 새 자리를 맨 앞에
n /= 2;
}
return result;
}

// 방식 2: 뒤에 붙인 뒤 마지막에 뒤집기
string toBinary(int n) {
string result = "";
while (n > 0) {
result += char('0' + n % 2);
n /= 2;
}
reverse(result.begin(), result.end()); // <algorithm>
return result;
}
'''
