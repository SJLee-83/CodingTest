## 프로그래머스의 채점 방식

프로그래머스는 **`main`을 직접 만드는 방식이 아닙니다.** 채점 서버가 자기만의 `main`(테스트 하네스)을 따로 가지고 있고, 그 안에서 제출한 `solution(s)`를 여러 입력으로 호출한 뒤 리턴값을 정답과 비교합니다. 그래서 두 가지가 반드시 지켜져야 해요.

첫째, 함수 이름은 정확히 `solution`이어야 하고, 문제에서 정해준 매개변수·리턴 타입(여기선 `bool solution(string s)`)을 그대로 맞춰야 합니다. 이름이 `dec`이거나 시그니처가 다르면 위 링커 에러가 납니다.

둘째, `cin`으로 입력을 받거나 `main`을 만들 필요가 없습니다. 입력은 채점 서버가 인자로 넣어주니까요. `main`을 직접 정의하면 채점 서버의 `main`과 충돌하기도 합니다.

참고로 `return dec(s);`도 의도와 다르게 동작하는데, `main`의 리턴값은 프로그램 종료 코드라서 `true`/`false`를 여기로 돌려줘도 정답 판정에는 전혀 쓰이지 않습니다. 로직의 결과는 오직 `solution`의 리턴값으로만 전달됩니다.


바꾼 부분을 정리하면, 함수 이름을 `dec` → `solution`으로 맞췄고, `main`과 `cin` 입력 부분을 통째로 제거했습니다. 또 전역에 있던 `cnt`를 함수 안으로 넣었어요. 전역 변수로 두면 채점할 때 같은 코드로 여러 케이스를 연달아 호출하는 경우 `cnt`가 초기화 안 된 채 누적될 위험이 있어서, 이런 상태값은 함수 안 지역 변수로 두는 게 안전합니다.

로직 자체(카운터가 음수면 false, 끝에 0이면 true)는 완벽하게 맞았어요. 문제는 순전히 "프로그래머스가 요구하는 함수 형태로 제출했는가"였습니다. 앞으로 프로그래머스 문제를 풀 땐 문제 페이지에 주어지는 **스켈레톤 코드(빈 `solution` 함수)를 그대로 두고 그 안만 채우는 습관**을 들이면 이 실수는 안 나옵니다.