# defaultdict
from collections import defaultdict # defaultdict(int)가 "키가 없으면 int()인 0으로 시작

def solution(participant, completion):
    count = defaultdict(int) # 없는 키는 자동으로 0

# Counter
from collections import Counter

Counter(participant) -> {'mislav': 2, 'stanko': 1, 'ana': 1}

# startswith
startswith라는 접두어 전용 메서드가 있어요. b.startswith(a)는 "b가 a로 시작하는가"를 바로 답해줍니다

# set
book = {"119", "97674223", "1195524421", "12"}
print(book)
    # {'12', '97674223', '119', '1195524421'}   ← 순서 없음